---
## MANDATORY COVERAGE CONTRACT (NON-NEGOTIABLE)

You MUST implement the requested feature with **complete transitive coverage** across:
- All connected Information Elements (IEs) and referenced ASN.1 types
- All container / parent chain structures that carry those IEs
- All encoder + decoder paths (producer + consumer)
- All call-sites and dependent code that uses the decoded/encoded structures

Partial / shallow changes are forbidden. If IE-A implies IE-B (via ASN.1 type reference, container membership, SEQUENCE field, CHOICE alternative, SEQUENCE OF item type, ProtocolIE container, or extension container), you MUST update **both** the representation and all affected encode/decode + consumer code.

### Required phases (you MUST follow in order)

#### Phase 0 — Dependency Closure (Depth-First)
Before modifying any file, you MUST compute a **Dependency Closure** starting from the message root(s) implied by the user request and the provided template JSON.

You MUST output a machine-checkable JSON object named `dependency_closure_report` containing:
- `roots`: list of root container(s) (message IE container / top-level PDU / top-level C struct(s) used for this message)
- `nodes`: list of **all** reachable IEs/types (depth-first), each with:
  - `name`
  - `kind`: `IE|ASN_TYPE|C_STRUCT|ENCODER|DECODER|CALLSITE`
  - `depth`: integer (root is 0)
  - `parents`: list of parent node names (for traceability)
- `edges`: list of `{ "from": "...", "to": "...", "reason": "field|choice|list|container|extension|encode|decode|uses|calls" }`
- `max_depth`
- `unresolved_symbols`: list (MUST be empty before coding starts; if not empty you MUST keep searching/expanding until it is empty)

Rules:
- The closure MUST include all connected types/IEs, not only those explicitly listed in the user request.
- For ASN.1: treat `::=` definitions as authoritative; traverse until primitives.
- For code: traverse `struct` fields, encode/decode helper usage, and consumer call-sites.
- You MUST include BOTH directions for code impact: producers (encode/build) AND consumers (decode/use).

#### Phase 1 — Impact Matrix (No Orphans Allowed)
You MUST output an `impact_matrix` that maps every closure node (except primitives) to concrete code changes:
- Struct definition locations (.h/.c)
- Encoder/decoder functions to update
- Helper/init/copy/free functions to update (as required by project patterns)
- All consumer/call-site locations that read/write the fields

Hard rule: **No orphan nodes**.
- If a node is in dependency closure and has no planned code touchpoints, that is a failure: keep searching until you find the right integration points.

#### Phase 2 — Implementation (Propagation Required)
Implement the feature and propagate changes through:
- All structs/types from the closure
- Encode + decode paths
- All call-sites in each actor that handle or consume the message

Hard rule: For every optional IE you add/change, you MUST implement presence tracking and wire it through encode/decode + consumer logic.

#### Phase 3 — Self-Audit (Coverage Gate)
After coding, you MUST run a self-audit and output `coverage_gate_report`:
- `closure_nodes_total`
- `closure_nodes_covered`
- `missing_nodes` (MUST be empty)
- `max_depth_covered` (MUST equal `dependency_closure_report.max_depth`)
- `evidence`: for each node, cite the exact file + symbol/function touched

If anything is missing, you MUST do another patch round until coverage is complete.

### Prohibited behaviors
- Do NOT “skip” connected IEs because they seem generic.
- Do NOT update only encode or only decode.
- Do NOT update only one actor when multiple actors consume/produce the message.
- Do NOT stop after updating a single struct if there are downstream consumers.

---

---
# Role Definition

You are a **Senior Software Engineer with deep domain expertise in 5G NR RAN protocol stack implementation**, specializing in F1AP protocol message handling, ASN.1-based protocol IE expansions, and enterprise-grade production software development. You possess comprehensive knowledge of 3GPP TS 38.401 specifications, OpenAirInterface 5G codebase architecture, and rigorous software engineering practices for telecom protocol stacks.

---

# Task Overview

You are tasked with **implementing the Inter-gNB-DU LTM Handover feature** focusing on the preparation, transmission, reception, and processing of the F1AP messages:

- **UE CONTEXT SETUP REQUEST** message sent by the gNB-CU to the candidate gNB-DU.
- **UE CONTEXT SETUP RESPONSE** message sent by the candidate gNB-DU back to the gNB-CU.
- Handling the UE CONTEXT SETUP RESPONSE message on the gNB-CU side to complete the LTM handover procedure.

This implementation must be **production-ready, fully integrated, and compliant with 3GPP TS 38.401 Release 18 Section 8.2.1.5**, reflecting enterprise-level rigor and completeness.

---

# Authoritative Input Context Description

- The feature is defined in 3GPP TS 38.401, Section 8.2.1.5, describing Inter-gNB-DU LTM handover procedures.
- The procedure involves F1AP control plane messages exchanged between gNB-CU and gNB-DU over the F1-C interface.
- The filled JSON specification provides detailed feature description, objectives, design details, call flow, procedure steps, relevant code artifacts, and Information Elements (IEs) involved.
- The main actors are **gNB-CU** (sender of UE CONTEXT SETUP REQUEST and receiver of UE CONTEXT SETUP RESPONSE) and **candidate gNB-DU** (receiver of UE CONTEXT SETUP REQUEST and sender of UE CONTEXT SETUP RESPONSE).
- The relevant codebase is **OpenAirInterface 5G**, with specific files and data structures identified for modification.
- The ASN.1 definitions for F1AP messages and IEs are the **single source of truth** for IE structure and type definitions.
- The implementation must handle **full recursive ASN.1 IE expansion** for all IEs relevant to these messages, including optional and conditional fields.
- The implementation must consider **special cases** where ASN.1 definitions or generated code may not yet include Rel-18 LTM-specific IEs, requiring internal modeling without breaking compilation or type safety.

---

# Actor-Specific Codebase Rules

- Only modify code within folders and files corresponding to the actors in the call flow:
  - **gNB-CU**: primarily `openair2/RRC/NR/` and related F1AP message handling files.
  - **gNB-DU**: primarily `openair2/LAYER2/NR_MAC_gNB/` and related F1AP message handling files.
- Do **NOT** modify unrelated actors or layers.
- Maintain strict separation of concerns between gNB-CU and gNB-DU code.
- Follow existing codebase conventions for each actor’s message handling, data structures, and helper functions.

---

# Type & Struct Alignment Enforcement

- All C struct definitions must **exactly match** the ASN.1 IE field names, types, and constraints.
- For each IE:
  - If `SEQUENCE`, expand all fields recursively.
  - If `SEQUENCE OF`, define appropriate list structures with cardinality constraints.
  - If `CHOICE`, implement all alternatives with discriminators.
  - If `ENUMERATED`, define enums with all values.
  - If `INTEGER` with range, enforce range validation.
  - If `BIT STRING` with size, enforce bit length and mask handling.
  - If `OCTET STRING`, preserve encoding semantics and size constraints.
- Implement presence bitmasks or flags for optional IEs and wire them through encode/decode and helper functions.
- Maintain **value vs pointer semantics** consistent with local headers and existing code.
- Ensure nested containers and extension containers are fully represented with correct ownership and lifetime management.

---

# Implementation Strategy (Phased Approach)

**Phase 1: Specification Analysis**

- Parse the filled JSON specification to extract:
  - Actors involved.
  - Message types and directions.
  - Top-level root IE container(s) for UE CONTEXT SETUP REQUEST and RESPONSE.
  - All IEs listed in the JSON and their ASN.1 definitions.
  - Constraints, optionality, and criticality rules.
- Identify ASN.1 files and generated headers corresponding to these IEs.
- Identify any ASN.1 version gaps or missing Rel-18 IEs.

**Phase 2: Codebase Integration & Modification Points Identification**

- Locate all existing code artifacts related to the messages:
  - Data structures (`f1ap_ue_context_setup_req_s`, `f1ap_ue_context_setup_resp_s`).
  - Helper functions (`ue_context_setup_request_f1ap`, `rrc_gNB_generate_UeContextSetupRequest`, `ue_context_setup_response_f1ap`).
- Identify all files that decode, encode, and consume these messages.
- Map each IE and nested IE to exact code locations for struct definitions, encode/decode logic, helper functions, and call-sites.
- Identify all call-sites that use these messages and their data structures for update.
- Document all integration points for both gNB-CU and gNB-DU actors.

**Phase 3: Code Generation & Codebase Modifications**

- Implement full recursive ASN.1 IE expansions for all relevant IEs starting from the root IE containers.
- Generate or update C struct definitions for all IEs, nested IEs, and extension containers.
- Implement or update encode/decode functions for all expanded IEs, including presence flags, list iteration, and conditional fields.
- Implement helper functions for copying, initialization, and validation of all new or updated IEs.
- Update message preparation functions on gNB-CU to populate UE CONTEXT SETUP REQUEST with LTM-specific configurations.
- Update message reception and parsing functions on gNB-DU to handle UE CONTEXT SETUP REQUEST and generate UE CONTEXT SETUP RESPONSE.
- Update gNB-CU reception functions to handle UE CONTEXT SETUP RESPONSE and process lower layer RRC configurations.
- Enforce all constraints, criticality handling, and error handling as per specification.
- Implement memory safety measures: nested allocation/deallocation, ownership transfer, and resource cleanup.
- Add comprehensive inline documentation and TODO comments for any ASN.1 version gaps or future upgrades.

---

# Codebase Modification Identification Process

- Perform a **comprehensive search** for all references to the involved data structures and functions in the codebase.
- Identify all header files defining the structs and IEs.
- Identify all source files implementing encode/decode and helper functions.
- Identify all call-sites that send, receive, or process these messages.
- Document exact file paths, line ranges, and function names for every modification.
- Ensure no code artifacts related to these messages are left unexamined or unmodified if impacted.
- Maintain actor-specific boundaries and do not cross-modify unrelated code.

---

# Mandatory Data Structure Updates

- Update `f1ap_ue_context_setup_req_s` to include all Rel-18 LTM-specific IEs:
  - CandidatecellID
  - CSIresourceconfiguration
  - LTMconfigurationIDmappinglist (optional)
  - PRACHresources (optional)
  - Lowerlayerconfiguration (optional)
- Update `f1ap_ue_context_setup_resp_s` to include:
  - LowerlayerRRCconfigurations
- For each IE, define nested structs as per ASN.1 expansions.
- Implement presence bitmasks for optional IEs.
- Ensure all list types have size fields and cardinality validation.
- Add any necessary extension containers or vendor-specific fields for future ASN.1 upgrades.

---

# IE / ASN / Encoding Handling Rules

- Identify the **root IE containers** for UE CONTEXT SETUP REQUEST and RESPONSE messages from the filled JSON and ASN.1 definitions.
- Perform **strict depth-first recursive traversal** of the ASN.1 dependency tree for these root IEs:
  - Expand all nested SEQUENCE, SEQUENCE OF, CHOICE, ENUMERATED, INTEGER, BIT STRING, OCTET STRING, ProtocolIE containers, and extension containers.
- Cross-check every IE name mentioned in the JSON against the ASN.1 tree; all must be resolved.
- For each IE and nested IE:
  - Generate or update C struct definitions aligned with ASN.1.
  - Generate or update encode/decode functions with full field mapping.
  - Implement presence and criticality handling as per 3GPP rules.
  - Enforce range and size constraints at runtime.
- For extension containers:
  - Implement extension-safe handling with recursive resolution.
- For any ASN.1 version gaps:
  - Do not reference missing ASN.1-generated symbols.
  - Model missing IEs internally with new structs and helper functions.
  - Use existing extension hooks or vendor-specific fields.
  - Mark these places clearly with TODO comments.
- Ensure encoding/decoding preserves protocol semantics and is interoperable with standard-compliant peers.

---

# Consumer / Call-Site Update Requirements

- Update all call-sites that:
  - Prepare and send UE CONTEXT SETUP REQUEST messages on gNB-CU.
  - Receive and process UE CONTEXT SETUP REQUEST messages on gNB-DU.
  - Prepare and send UE CONTEXT SETUP RESPONSE messages on gNB-DU.
  - Receive and process UE CONTEXT SETUP RESPONSE messages on gNB-CU.
- Update all RRC layer functions that generate or consume the LTM-specific configurations.
- Update all MAC and F1AP layer functions that handle message encoding, transmission, and reception.
- Ensure all call-sites handle optional fields, presence flags, and error conditions robustly.
- Update unit tests and integration tests to cover new message fields and processing logic.
- Document all call-site changes with file paths and function names.

---

# Memory Safety & Resource Management Requirements

- Implement nested allocation and deallocation for all dynamically sized IEs (e.g., SEQUENCE OF).
- Ensure ownership transfer semantics are clearly defined and enforced.
- Implement robust cleanup paths on error or partial failure.
- Avoid memory leaks, dangling pointers, and double frees.
- Use existing project memory management utilities and conventions.
- Validate all input sizes and ranges before allocation.
- Implement thread-safe handling if applicable.
- Document memory ownership and lifecycle in code comments.

---

# Compilation Safety & Type Resolution Rules

- All new or updated code must compile cleanly with existing project build system.
- Use only types and symbols defined in the local actor’s headers or generated ASN.1 code.
- Do not introduce cross-actor type dependencies.
- Avoid forward declarations that break type safety.
- Enforce strict type alignment with ASN.1-generated types.
- Use compiler warnings and static analysis tools to verify type correctness.
- Document any deviations or workarounds due to ASN.1 version gaps.

---

# Code Quality & Naming Standards

- Follow existing OpenAirInterface 5G coding style and naming conventions.
- Use descriptive, consistent names matching ASN.1 IE names exactly.
- Use camelCase or snake_case as per local conventions.
- Document all new functions and data structures with clear comments.
- Avoid code duplication by reusing existing helper functions where possible.
- Implement comprehensive error handling with meaningful error codes.
- Ensure performance is optimized for runtime encoding/decoding.
- Use assertions and validation macros to enforce invariants.
- Maintain modularity and separation of concerns.

---

# Error Handling & Validation Rules

- Validate all input parameters and IE fields for correctness and completeness.
- Enforce mandatory presence of required IEs.
- Enforce range and size constraints on all fields.
- Handle optional and conditional IEs gracefully.
- Implement criticality handling as per 3GPP TS 38.401 rules:
  - Reject messages with critical errors.
  - Ignore non-critical unknown IEs.
- Return detailed error codes on failures.
- Log errors with sufficient context for debugging.
- Implement retry or fallback mechanisms where applicable.
- Ensure no partial or incomplete message processing.

---

# Output Format Requirements

- Provide **no source code** in this prompt.
- Output must be a **comprehensive, structured, and exhaustive instruction set** for the implementing LLM.
- Use clear section headings and bullet points.
- Include explicit file paths, function names, and line ranges for all modifications.
- Provide detailed instructions for data structure updates, encode/decode logic, helper functions, and call-site changes.
- Include validation checklists and compliance verification steps.
- Provide integration summary and final engineering checklist.
- Use imperative language (MUST, REQUIRED, CRITICAL).
- Ensure the output is at least 3–5 pages in length, favoring over-specification.

---

# Integration Summary Requirements

- Summarize all integration points for gNB-CU and gNB-DU actors.
- Detail how the new or updated messages integrate with existing RRC, MAC, and F1AP layers.
- Specify how the LTM configuration data flows from UE measurement reports to handover execution.
- Document any required configuration or build system changes.
- Provide instructions for testing and validation of the new feature.
- Include notes on future ASN.1 upgrades and extension points.
- Provide TODO comments and integration notes for any ASN.1 version gaps.

---

# Final Engineering Checklist

- [ ] All root IE containers for UE CONTEXT SETUP REQUEST and RESPONSE identified and expanded recursively.
- [ ] All referenced IEs and types resolved and mapped to code artifacts.
- [ ] All C structs updated or generated with exact ASN.1 alignment.
- [ ] Encode/decode functions updated for all expanded IEs.
- [ ] Helper functions for copy, init, validation implemented.
- [ ] Call-sites updated for message preparation, sending, reception, and processing.
- [ ] Memory safety and resource management implemented and verified.
- [ ] Compilation safety and type correctness verified.
- [ ] Code quality and naming standards enforced.
- [ ] Error handling and validation rules implemented.
- [ ] Integration summary documented.
- [ ] Coverage contract enforced: dependency closure report, impact matrix, self-audit coverage gate passed.
- [ ] No partial implementation allowed; full feature coverage mandatory.
- [ ] ASN.1 version gaps handled with internal modeling and TODO comments.
- [ ] No modifications to .asn files; all changes confined to codebase.

---

# Critical Reminders Section

- **Partial or shallow implementation is strictly forbidden.** You MUST implement the entire dependency closure of ASN.1 IEs for the messages involved.
- **ASN.1 definitions are the single source of truth.** Never rely solely on the natural-language IE list.
- **Recursive, depth-first IE expansion is mandatory.** No IE or nested IE can be left unresolved.
- **Maintain strict actor boundaries.** Only modify code relevant to gNB-CU and candidate gNB-DU as per call flow.
- **Memory safety and resource management are critical.** No memory leaks or unsafe pointer usage allowed.
- **Error handling must be robust and comprehensive.**
- **Do not modify ASN.1 (.asn) files.** Document any required third-party changes.
- **Document all integration points, TODOs, and future upgrade paths clearly.**

---

# Recursive ASN.1 Information Element Expansion Requirements

- Identify the **top-level root IE containers** for the UE CONTEXT SETUP REQUEST and UE CONTEXT SETUP RESPONSE messages from the ASN.1 definitions.
- For each root IE container:
  - Extract the ASN.1 definition from the official TS 38.401 ASN.1 module.
  - Recursively expand every referenced IE and TYPE:
    - For `SEQUENCE`, expand all fields.
    - For `SEQUENCE OF`, expand the item type recursively.
    - For `CHOICE`, expand all alternatives.
    - For `ENUMERATED`, enumerate all values.
    - For `INTEGER` with range, enforce range validation.
    - For `BIT STRING` with size, enforce bit length and mask handling.
    - For `OCTET STRING`, preserve encoding semantics and size constraints.
    - For `ProtocolIE-SingleContainer` and `ProtocolIE-Container`, unwrap and continue recursion.
    - For `ProtocolExtensionContainer`, include extension-safe handling and recursive resolution.
- Cross-verify that every IE name mentioned in the filled JSON is included in the recursive expansion.
- Generate or update C struct definitions and encode/decode logic for every resolved IE.
- Implement presence bitmasks and criticality handling as per ASN.1 and 3GPP rules.
- For any missing ASN.1 definitions (Rel-18 LTM IEs not present in current ASN.1), implement internal modeling with clear TODO notes.

---

# Mandatory Coverage Contract (Dependency Closure + Impact Matrix + Self-Audit Coverage Gate)

You MUST enforce the following **Coverage Contract** to prevent shallow or partial implementation:

- **Dependency Closure Report**: Before any code generation, produce a depth-first dependency closure report listing every ASN.1 IE and TYPE referenced by the root IE containers, including nested and extension IEs.

- **Impact Matrix**: Map every closure node (IE or TYPE) to concrete code touchpoints in the codebase:
  - Data structure definitions.
  - Encode/decode functions.
  - Helper functions.
  - Call-sites and consumers.

- **Full Implementation**: Implement changes for **all** closure nodes. No orphan nodes or unimplemented IEs allowed.

- **Self-Audit Coverage Gate**: After implementation, run a self-audit that verifies:
  - All closure nodes are implemented.
  - All call-sites updated.
  - All encode/decode paths covered.
  - All optional and conditional fields handled.
  - Memory safety and error handling enforced.

- **Producer and Consumer Coverage**: Cover both encoding/build (producer) and decoding/use (consumer) code paths fully.

- **No Partial Implementation**: Partial or incremental implementation is strictly forbidden.

- **Documentation**: Document the coverage report, impact matrix, and audit results in the integration summary.

---

# MANDATORY ENDING STRUCTURE

## 1. Role

Senior engineer persona with domain expertise.

## 2. TASK

- **CRITICAL REQUIREMENT:** Implement full Inter-gNB-DU LTM handover support by preparing and sending the F1AP UE CONTEXT SETUP REQUEST message from gNB-CU to candidate gNB-DU, handling the UE CONTEXT SETUP RESPONSE message on gNB-CU, and processing the response on gNB-DU. All code changes MUST be made in the exact locations identified in the OpenAirInterface 5G codebase, respecting actor boundaries and existing call flows.

- **IE NAMING REQUIREMENT:** All IE names MUST exactly match those defined in the ASN.1 specification and the filled JSON template, including but not limited to UEContextSetupRequestIEs, UEContextSetupResponseIEs, LTMconfigurationIDmappinglist, CSIresourceconfiguration, CandidatecellID, PRACHresources, Lowerlayerconfiguration, and LowerlayerRRCconfigurations.

- **TYPE AND STRUCT DEFINITION ALIGNMENT (PER ACTOR):** For each actor (gNB-CU and candidate gNB-DU), all C struct member names, list/container shapes, and pointer/value semantics MUST align precisely with the ASN.1 definitions and existing local headers. Optional fields MUST be represented with presence bitmasks.

- **CONSUMER / CALL-SITE CODE (PER ACTOR):** Update all files and functions that consume or produce the decoded message structs per actor, including message preparation, encoding, sending, reception, decoding, and processing logic.

- **ACTOR-SPECIFIC CODEBASE REQUIREMENT:** Only modify code within folders and files corresponding to the actors in the call flow: gNB-CU and candidate gNB-DU. Do NOT modify unrelated actors or layers.

- **ASN.1 FILES (.asn) - THIRD PARTY:** Do NOT modify any ASN.1 (.asn) files. Document any required changes or feature gaps for third-party ASN.1 tooling or code generators in the integration summary.

## 3. INPUT CONTEXT

- The filled JSON template provides detailed feature description, objectives, call flow, procedure steps, code artifacts, and IE lists.
- The existing OpenAirInterface 5G codebase contains relevant data structures and helper functions primarily in:
  - `openair2/COMMON/f1ap_messages_types.h`
  - `openair2/RRC/NR/mac_rrc_dl_f1ap.c`
  - `openair2/RRC/NR/rrc_gNB.c`
  - `openair2/LAYER2/NR_MAC_gNB/mac_rrc_ul_f1ap.c`
- ASN.1 definitions from 3GPP TS 38.401 are the authoritative source for IE structures.
- ASN.1 version gaps for Rel-18 LTM IEs may exist and must be handled internally without breaking compilation.

## 4. IMPLEMENTATION STRATEGY

- **Phase 1: Specification Analysis**
  - Parse JSON to extract actors, messages, root IE containers, and constraints.
  - Locate ASN.1 definitions and identify any version gaps.

- **Phase 2: Codebase Integration & Modification Points Identification**
  - Locate all relevant data structures, encode/decode functions, helper functions, and call-sites.
  - Map every IE and nested IE to exact code locations.
  - Document integration points per actor.

- **Phase 3: Code Generation & Codebase Modifications**
  - Perform full recursive ASN.1 IE expansion.
  - Update/generate C structs, encode/decode, helper functions.
  - Update message preparation and processing logic.
  - Enforce memory safety, error handling, and validation.
  - Document all changes and TODOs for ASN.1 gaps.

## 5. CODE QUALITY REQUIREMENTS

- Follow OpenAirInterface 5G coding style and naming conventions.
- Enforce strict type safety and alignment with ASN.1.
- Implement robust error handling and validation.
- Optimize for performance and maintainability.
- Ensure compilation safety with no cross-actor type leaks.
- Document all new code thoroughly.

## 6. OUTPUT FORMAT

- Provide detailed instructions for:
  - Primary function implementations.
  - Data structure modifications.
  - ASN.1/IE updates (internal only, no .asn file changes).
  - Encoding/decoding logic updates.
  - Header file updates.
  - Helper function implementations.
  - Dependency closure and impact matrix documentation.
  - Integration summary with file paths and function names per actor.

## 7. SPECIAL CONSIDERATIONS

- Protocol compliance with 3GPP TS 38.401 Release 18.
- Encoding/decoding must preserve protocol semantics.
- State management and concurrency considerations as per existing codebase.
- Testing hooks and validation points must be included.
- Handle ASN.1 version gaps with internal modeling and clear TODO comments.

## 8. FINAL CHECKLIST

- [ ] Full recursive ASN.1 IE expansion completed.
- [ ] All data structures updated with exact ASN.1 alignment.
- [ ] Encode/decode and helper functions implemented.
- [ ] Call-sites updated for both gNB-CU and candidate gNB-DU.
- [ ] Memory safety and resource management enforced.
- [ ] Compilation safety and type correctness verified.
- [ ] Code quality and naming standards met.
- [ ] Error handling and validation implemented.
- [ ] Coverage contract fulfilled: dependency closure, impact matrix, self-audit.
- [ ] Integration summary documented.
- [ ] No partial implementation.
- [ ] ASN.1 version gaps handled internally.
- [ ] No .asn file modifications.

## 9. Closing reminder

The implementation MUST be complete and fully integrated at the exact locations identified in the OpenAirInterface 5G codebase for both gNB-CU and candidate gNB-DU actors. All related components including data structures, encode/decode logic, helper functions, and call-sites MUST be updated consistently. ASN.1 (.asn) files MUST NOT be modified in the codebase. Any ASN.1 version gaps MUST be handled internally with clear documentation and TODO notes for future upgrades.

Here is the template :{
  "Feature_ID": "INTER_GNB_DU_LTM_HANDOVER",
  "Feature_Name": "Inter-gNB-DU LTM Handover",
  "Release": "18",
  "3GPP_Reference": {
    "Spec": "TS 38.401",
    "Title": "NG-RAN Architecture Description",
    "Section": "8.2.1.5"
  },
  "Feature_Description": [
    "This procedure is used for the case when the UE moves from one gNB-DU to another gNB-DU within the same gNB-CU during NR operation for LTM."
  ],
  "Feature_Objectives": [
    "To enable the gNB-CU to initiate LTM configuration for a UE moving between gNB-DUs.",
    "To create a UE context and set up one or more data bearers in the candidate gNB-DU for LTM.",
    "To allow the gNB-CU to provide LTM-specific configurations (e.g., CSI resource configuration, LTM configuration ID mapping list) to the candidate gNB-DU."
  ],
  "Design_Details": {
    "Protocol_Layers": [
      "F1AP (Control Plane)",
      "RRC (Radio Resource Control)",
      "F1-U (User Plane)"
    ],
    "Protocol_Interfaces": [
      "F1-C (gNB-CU to gNB-DU)",
      "F1-U (gNB-CU to gNB-DU)",
      "Uu (UE to gNB-DU)"
    ]
  },
  "Feature_Implementation_Steps": [
    "1. The UE sends a MeasurementReport message (L3 measurement result) to the source gNB-DU containing measurements of neighbouring cells. The source gNB-DU sends an UL RRC MESSAGE TRANSFER message conveying the received MeasurementReport message to the gNB-CU.",
    "2. The gNB-CU determines to initiate LTM configuration.",
    "3. The gNB-CU sends a UE CONTEXT SETUP REQUEST message to the candidate gNB-DU(s) for each candidate cell, containing one candidate cell ID and the CSI resource configuration for subsequent LTM. The gNB-CU may provide the LTM configuration ID mapping list to the candidate gNB-DU(s). The gNB-CU may request PRACH resources from the candidate gNB-DU(s). The gNB-CU may request the candidate gNB-DU(s) to provide the lower layer configuration for the purpose of generating the reference configuration or provide the lower layer part of the reference configuration to the candidate gNB-DU(s).",
    "4. If the candidate gNB-DU accepts the request of LTM configuration, it responds with a UE CONTEXT SETUP RESPONSE message including the generated lower layer RRC configurations for the accepted target candidate cell."
  ],
  "Call_Flow": [
    {
      "Message": "UL RRC MESSAGE TRANSFER",
      "Actors": [
        "Source gNB-DU",
        "gNB-CU"
      ],
      "From": "Source gNB-DU",
      "To": "gNB-CU"
    },
    {
      "Message": "UE CONTEXT SETUP REQUEST",
      "Actors": [
        "gNB-CU",
        "Candidate gNB-DU"
      ],
      "From": "gNB-CU",
      "To": "Candidate gNB-DU"
    },
    {
      "Message": "UE CONTEXT SETUP RESPONSE",
      "Actors": [
        "Candidate gNB-DU",
        "gNB-CU"
      ],
      "From": "Candidate gNB-DU",
      "To": "gNB-CU"
    }
  ],
  "Procedure_Implementation_Steps": [
    "1. The UE sends a MeasurementReport message (L3 measurement result) to the source gNB-DU, which forwards it to the gNB-CU via an UL RRC MESSAGE TRANSFER message.",
    "2. The gNB-CU analyzes the MeasurementReport and determines to initiate LTM configuration for the UE.",
    "3. The gNB-CU prepares and sends an F1AP UE CONTEXT SETUP REQUEST message to the candidate gNB-DU(s) for each candidate cell. This message includes the candidate cell ID, CSI resource configuration for subsequent LTM, and may optionally include the LTM configuration ID mapping list, a request for PRACH resources, and/or the lower layer configuration (or part of it) for generating the reference configuration.",
    "4. Upon receiving the UE CONTEXT SETUP REQUEST, the candidate gNB-DU processes the request. If it accepts the LTM configuration, it responds to the gNB-CU with an F1AP UE CONTEXT SETUP RESPONSE message. This response includes the generated lower layer RRC configurations for the accepted target candidate cell."
  ],
  "Code_Artifacts": [
    {
      "Codebase_Name": "OpenAirInterface 5G",
      "Code_Instructions": [
        {
          "type": "data_structure",
          "struct_name": "f1ap_ue_context_setup_req_s",
          "file_path": "openair2\\COMMON\\f1ap_messages_types.h",
          "line_start": 544,
          "description": "Data structure defining the format of the F1AP UE Context Setup Request message, used by the gNB-CU to initiate UE context setup at the gNB-DU for LTM handover."
        },
        {
          "type": "data_structure",
          "struct_name": "f1ap_ue_context_setup_resp_s",
          "file_path": "openair2\\COMMON\\f1ap_messages_types.h",
          "line_start": 570,
          "description": "Data structure defining the format of the F1AP UE Context Setup Response message, used by the gNB-DU to acknowledge and provide configuration details to the gNB-CU after receiving a UE Context Setup Request."
        },
        {
          "type": "helper_function",
          "function_name": "ue_context_setup_request_f1ap",
          "file_path": "openair2\\RRC\\NR\\mac_rrc_dl_f1ap.c",
          "line_start": 79,
          "line_end": 80,
          "description": "Helper function responsible for preparing and potentially encoding the F1AP UE Context Setup Request message from the RRC layer for transmission to the F1AP layer and subsequently to the gNB-DU.",
          "called_functions": [],
          "calling_functions": [],
          "uses_structs": [
            "f1ap_ue_context_setup_req_s"
          ]
        },
        {
          "type": "helper_function",
          "function_name": "rrc_gNB_generate_UeContextSetupRequest",
          "file_path": "openair2\\RRC\\NR\\rrc_gNB.c",
          "line_start": 3032,
          "line_end": 3033,
          "description": "Helper function within the gNB RRC layer responsible for generating the RRC-level information and parameters (e.g., CSI resource configuration, LTM configuration ID mapping, PRACH resources) required to populate the F1AP UE Context Setup Request message.",
          "called_functions": [],
          "calling_functions": [],
          "uses_structs": [
            "f1ap_ue_context_setup_req_s"
          ]
        },
        {
          "type": "helper_function",
          "function_name": "ue_context_setup_response_f1ap",
          "file_path": "openair2\\LAYER2\\NR_MAC_gNB\\mac_rrc_ul_f1ap.c",
          "line_start": 89,
          "line_end": 90,
          "description": "Helper function responsible for receiving and parsing the F1AP UE Context Setup Response message from the F1AP layer, extracting relevant information (e.g., lower layer RRC configurations) and forwarding it to the RRC layer for further processing.",
          "called_functions": [],
          "calling_functions": [],
          "uses_structs": [
            "f1ap_ue_context_setup_resp_s"
          ]
        }
      ]
    }
  ],
  "ASN_IES_File_Path": [],
  "ASN_IES_C_Structures_Path": [],
  "Information_Elements": [
    {
      "IE_Name": "UEContextSetupRequestIEs",
      "IE_Definition": "",
      "IE_Category": "F1AP IE"
    },
    {
      "IE_Name": "UEContextSetupResponseIEs",
      "IE_Definition": "",
      "IE_Category": "F1AP IE"
    },
    {
      "IE_Name": "LTMconfigurationIDmappinglist",
      "IE_Definition": "",
      "IE_Category": "F1AP IE"
    },
    {
      "IE_Name": "CSIresourceconfiguration",
      "IE_Definition": "",
      "IE_Category": "F1AP IE"
    },
    {
      "IE_Name": "CandidatecellID",
      "IE_Definition": "",
      "IE_Category": "F1AP IE"
    },
    {
      "IE_Name": "PRACHresources",
      "IE_Definition": "",
      "IE_Category": "F1AP IE"
    },
    {
      "IE_Name": "Lowerlayerconfiguration",
      "IE_Definition": "",
      "IE_Category": "F1AP IE"
    },
    {
      "IE_Name": "LowerlayerRRCconfigurations",
      "IE_Definition": "",
      "IE_Category": "F1AP IE"
    }
  ],
  "Piggy_Backed_IES_Info": "",
  "Piggy_Backed_IES_Mapping": [],
  "Codebase_Metadata": {
    "Code_Language": "Python",
    "Build_System": "Unknown",
    "Repo_Path": ".",
    "Code_Output_Type": "Library/Executable",
    "Execution_Mode": "Runtime",
    "CI_Pipeline": "",
    "Version_Control": "Unknown"
  },
  "Knowledge_Hints": [
    "8.2.1.5 (ts_138401v180600p): Inter-gNB-DU LTM",
    "8.2.1 (ts_138401v180600p): Intra-NR Mobility",
    "8.2.1.6 (ts_138401v180600p): LTM with gNB-CU-UP change",
    "8.2.1.4 (ts_138401v180600p): Intra-gNB-DU LTM",
    "8.2.1.3 (ts_138401v180600p): Inter-gNB-DU Conditional Handover or Conditional PSCell Change or subsequent CPAC",
    "8.2.1.2 (ts_138401v180600p): Intra-gNB-DU handover",
    "8.2.1.1 (ts_138401v180600p): Inter-gNB-DU Mobility",
    "8.14.1.1 (ts_138401v180600p): Signalling of RLF information from gNB-CU to gNB-DU",
    "8.2 (ts_138401v180600p): Intra-gNB-CU Mobility",
    "8.2.2 (ts_138401v180600p): EN-DC Mobility",
    "8.2.3 (ts_138401v180600p): Intra-CU topology adaptation procedure",
    "8.2.5 (ts_138401v180600p): Intra-CU Backhaul RLF recovery for IAB-nodes in SA mode",
    "8.2.4 (ts_138401v180600p): Intra-CU topological redundancy procedure",
    "8.15.2 (ts_138401v180600p): Mobility procedure for Multicast",
    "8.2.2.2 (ts_138401v180600p): Inter-gNB-DU Mobility using SCG SRB (SRB3)",
    "8.9.4 (ts_138401v180600p): Inter-gNB handover involving gNB-CU-UP change",
    "8.9.5 (ts_138401v180600p): Change of gNB-CU-UP",
    "8.19.4.2 (ts_138401v180600p): Intra-gNB-DU switch from direct to indirect path",
    "8.2.2.3 (ts_138401v180600p): Inter-gNB-DU Conditional PSCell Change using MCG SRB without MN negotiation",
    "8.2.2.1 (ts_138401v180600p): Inter-gNB-DU Mobility using MCG SRB",
    "8.16.1 (ts_138401v180600p): MN initiated Conditional PSCell Addition",
    "6.2.2 (ts_138401v180600p): gNB-DU ID",
    "8.3.1 (ts_138401v180600p): Centralized Retransmission in Intra gNB-CU Cases",
    "8.4.1.1 (ts_138401v180600p): EN-DC",
    "8.4.2.1 (ts_138401v180600p): EN-DC",
    "8.14.1.3 (ts_138401v180600p): Reporting of DL LBT failure information from gNB-DU to gNB-CU",
    "6.2.5 (ts_138401v180600p): RAN UE ID",
    "8.19.4.1 (ts_138401v180600p): Inter-gNB-DU switch from direct to indirect path",
    "8.14.1 (ts_138401v180600p): Overall procedures for MRO",
    "8.14.1.2 (ts_138401v180600p): Signalling of RACH information from gNB-CU to gNB-DU"
  ]
}